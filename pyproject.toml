[tool.poetry]
name = "trying-linear-regression"
version = "1.0.0"
description = "🌬🌬🌬 Pythonで線形回帰を実装してみる！"
authors = ["osawa-koki <koki@osawa.dev>"]
license = "Apache-2.0"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
